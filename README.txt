dash_app/
│── app.py                # Main entry point for the Dash app
│── index.py              # Handles page navigation
│── requirements.txt      # Dependencies
│── assets/               # Static files (CSS, JS, images)
│── components/           # Reusable UI components
│   │── navbar.py         # Navigation bar component
│   │── sidebar.py        # Sidebar component
│   │── graphs.py         # Reusable graph components
│── layouts/              # Page-specific layouts
│   │── home.py           # Home page layout
│   │── dashboard.py      # Dashboard page layout
│── pages/                # Callbacks for each page
│   │── dashboard_callbacks.py # Callbacks for dashboard
│── utils/                # Utility functions
│   │── data_loader.py    # Functions to fetch/process data
│── tests/                # Unit tests
